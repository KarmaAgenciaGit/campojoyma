services:
  agroiris:
    build: .
    container_name: campojoyma
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    networks:
    - proxy
networks:
  proxy:
    external: true
    name: proxy
